1. Create a new sheet called 'RawData'. Populate it with a header row (Agent Name, Calls Handled, Average Handle Time, CSAT Score) and 10 rows of realistic dummy data for a customer support team. Make the headers bold, white text on a dark blue background, and freeze the top row. - Pass
2. On the 'RawData' sheet, add a new column header in E1 called 'Performance Tier'. In E2, use a single ARRAYFORMULA that spills down to assign 'Top Performer' if the CSAT is > 90 and AHT is < 300, otherwise assign 'Standard'. Then, apply conditional formatting to the CSAT column to highlight values greater than 90 in green, and less than 80 in red. - Pass
3. Create a second sheet named 'Dashboard'. Add a merged, bold, centered title across A1:F1 called 'Team Performance Dashboard'. Create a summary table that calculates the total number of 'Top Performers' versus 'Standard' agents using data from the RawData sheet. Finally, insert a PIE chart below the table showing the distribution of Performance Tiers, and protect the entire 'Dashboard' sheet so it displays a warning when edited. - Pass
4. Rename the 'RawData' sheet to 'Q3_Metrics'. Auto-resize all columns in both sheets so the data fits perfectly. Add a checkbox data validation in cell H1 of the Dashboard labeled 'Reviewed. Pass
5. Create a new sheet called 'Config'. In column A, list three training statuses: 'Not Started', 'In Progress', and 'Completed'. Create a named range for these three cells called 'StatusList'. Then, go back to the 'Q3_Metrics' sheet, add a new column header called 'Training Status', and apply a dropdown data validation to its rows using the 'StatusList' named range you just created. - Pass
6. On the 'Dashboard' sheet, apply a thick black border around the summary table. Add a cell note to the 'Total Top Performers' cell that says 'Based on >90 CSAT and <300 AHT'. Finally, insert a public placeholder image (like https://via.placeholder.com/150) into cell F1, and change the text rotation of the Dashboard title to 15 degrees. - Pass
7. Duplicate the 'Dashboard' sheet and name the copy 'Executive_Summary'. Move the 'Executive_Summary' sheet to be the very first tab in the workbook. On this new sheet, clear the existing pie chart, and add a new COLUMN chart comparing 'Agent Name' against their 'Average Handle Time' using the data from the 'Q3_Metrics' sheet. - Pass
8. Let's do some cleanup. Unmerge the title row on the original 'Dashboard' sheet. Delete the 'Config' sheet entirely. Finally, clear the content (but keep the formatting and data validation) in the 'Training Status' column on the 'Q3_Metrics' sheet. - Pass
9. Create a new sheet called 'ErrorTest'. In cell A1, insert the number 100. In cell B1, write a formula that divides A1 by zero (e.g., =A1/0). Let's see how your error detection handles the resulting #DIV/0! error. - Pass
10. On the 'Q3_Metrics' sheet, protect the entire 'CSAT Score' column so it cannot be accidentally edited. Remove all existing editors, disable domain editing, and add 'admin@example.com' as the only allowed editor. Add a description to the protection saying 'Restricted CSAT Column' - Pass
11.First, clear all existing conditional formatting rules on the 'Q3_Metrics' sheet. Then, apply a new custom formula conditional formatting rule to the entire data range (A2:E11) that highlights the entire row in light yellow if the 'Average Handle Time' in that row is greater than 320. - Pass
12. Create a new sheet called 'CollisionTest'. First, merge cells A1:E5 together. Next, in the exact same single response, try to insert a 2D data table of random numbers spanning A1:C10. I want to see if your system properly shatters the merged cells to make room for the 2D grid. - Pass
